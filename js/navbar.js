// Get Reference To Element
var ul = document.querySelector("nav > ul");

// Define a behavior
function toggleMenu() {
  ul.classList.toggle("active");
}

// Wire it up!

// Use a shortcut and use the onclick attribute in HTML
